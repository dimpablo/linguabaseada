=== 📄 task1.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <title>Задание 1: Рисование</title>
  <script src="https://cdn.jsdelivr.net/npm/hanzi-writer@3.5/dist/hanzi-writer.min.js"></script>
  <style>
    body, html {
      margin: 0;
      padding: 0;
      width: 100%;
      height: 100%;
      font-family: 'Segoe UI', Arial, sans-serif;
      overflow: hidden;
    }

    #splash-screen {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: #f0f4ff;
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 1000;
      flex-direction: column;
      text-align: center;
      color: #333;
    }

    #splash-screen h1 {
      font-size: 3.5rem;
      font-weight: bold;
      color: #2c55d9;
      text-shadow: 2px 2px 8px rgba(0,0,0,0.1);
      margin: 0;
      animation: fadeIn 0.5s ease-in;
    }

    #splash-screen p {
      font-size: 1.5rem;
      color: #555;
      margin-top: 20px;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.9); }
      to { opacity: 1; transform: scale(1); }
    }

    #character-target-div {
      display: none;
      margin: 40px auto;
      width: 300px;
      height: 300px;
    }

    /* === Мерцающий overlay === */
    #feedback-overlay {
      pointer-events: none;
      position: fixed;
      inset: 0;
      opacity: 0;
      transition: opacity 0s;
      z-index: 9999;
    }

    @keyframes flash {
      0% { opacity: 0; }
      50% { opacity: 0.7; }
      100% { opacity: 0; }
    }

    #feedback-overlay.success {
      background: green;
    }

    #feedback-overlay.error {
      background: red;
    }

    #feedback-overlay.animate {
      opacity: 0.7;
      animation: flash 0.8s ease-out;
    }
  </style>
</head>
<body>

<!-- Заставка -->
<div id="splash-screen">
  <h1>Нарисуй здесь</h1>
  <p>любой иероглиф из словаря урока!</p>
</div>

<!-- Контейнер для HanziWriter -->
<div id="character-target-div"></div>

<!-- Overlay для мерцания -->
<div id="feedback-overlay"></div>

<script>
  // ✅ Универсальная функция отправки результата и баллов
  function sendResult(success, message, points = 0) {
    window.parent.postMessage({
      type: "TASK_RESULT",
      success: !!success,
      message: message || (success ? "Задание выполнено" : "Ошибка"),
      points: points // ✅ Отправляем баллы
    }, "*");
  }

  // ✅ Функция мерцания экрана
  function flashScreen(color) {
    const overlay = document.getElementById('feedback-overlay');
    overlay.className = '';
    overlay.classList.add(color === 'success' ? 'success' : 'error');
    overlay.classList.add('animate');

    setTimeout(() => {
      overlay.classList.remove('animate');
    }, 800);
  }

  let totalPointsEarned = 0;
  let strokeCount = 0;
  let mistakes = 0;

  window.onload = function() {
    const splashScreen = document.getElementById('splash-screen');
    const characterDiv = document.getElementById('character-target-div');
    const splashH1 = splashScreen.querySelector('h1');
    const splashP = splashScreen.querySelector('p');

    setTimeout(() => {
      const hash = window.location.hash.slice(1);
      if (!hash) {
        splashP.textContent = "⚠️ Не указан иероглиф";
        sendResult(false, "Не указан иероглиф", 0);
        return;
      }

      const char = decodeURIComponent(hash);

      // Показываем иероглиф
      splashH1.textContent = char;

      // === Попробуем загрузить данные для иероглифа ===
      HanziWriter.loadCharacterData(char)
        .then(charData => {
          // ✅ Данные есть — запускаем квиз
          splashScreen.style.display = 'none';
          characterDiv.style.display = 'block';

          // Считаем количество штрихов
          strokeCount = charData.strokes.length;

          const writer = HanziWriter.create('character-target-div', char, {
            width: 300,
            height: 300,
            showCharacter: false,
            showOutline: false,
            showHintAfterMisses: 1,
            highlightOnComplete: false,
            padding: 5
          });

          writer.quiz({
            leniency: 2.0,

            onCorrectStroke: function(strokeData) {
              // ✅ +1 балл за каждый правильный штрих
              totalPointsEarned += 1;
              console.log(`✅ Штрих ${strokeData.strokeNum} — +1 балл`);
            },

            onMistake: function(strokeData) {
              console.log("❌ Ошибка на штрихе", strokeData.strokeNum);
              flashScreen('error');
              mistakes++;
            },

            onComplete: function(summaryData) {
              console.log("🎯 Квиз завершён:", summaryData);
              flashScreen('success');

              // ✅ +2 балла за полное завершение
              totalPointsEarned += 2;

              sendResult(
                true,
                `Иероглиф "${char}" нарисован верно`,
                totalPointsEarned
              );
            }
          });

        })
        .catch(err => {
          // ❌ Данных нет — древний иероглиф
          console.warn(`🚫 Нет данных для иероглифа: ${char}`, err);

          // === ОСТАВЛЯЕМ ЗАСТАВКУ, МЕНЯЕМ ТЕКСТ ===
          splashP.innerHTML = `
            Очень древний и редкий иероглиф 😔<br>
            Софт не поддерживает его отрисовку.<br>
            <strong>Придётся выписать его от руки ✍️</strong>
          `;

          // Мерцание — ошибка
          flashScreen('error');

          // ✅ Ничего не штрафуем и не награждаем
          setTimeout(() => {
            sendResult(
              true,
              `Древний иероглиф "${char}" — задание пропущено (нет данных)`,
              0 // 🟢 0 баллов — не вина пользователя
            );
          }, 1200);
        });
    }, 1000);
  };

  // Обработка пропуска (например, если пользователь закрыл окно без завершения)
  window.addEventListener('beforeunload', () => {
    if (totalPointsEarned === 0 && mistakes > 0) {
      // ❌ Пропуск с ошибками — штраф -1
      sendResult(false, "Задание пропущено", -1);
    }
  });
</script>

<!-- Сигналы родительскому окну -->
<script>
  window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  window.parent.postMessage({ type: "DO_HANZI", valid: true }, "*");
</script>

</body>
</html>

=== 📄 task10.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Задание: 侯虎余其得汝</title>
</head>
<body>
  <h2>Что означает фраза «侯虎余其得汝»?</h2>
  <p><strong>Обратите внимание на личные местоимения.</strong></p>
  <div class="pinyin-container">
    <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
    <div class="pinyin-text">Hóu Hǔ yú qí dé rǔ</div>
  </div>
  <div id="answers"></div>
  <button onclick="check('answers', 'result', options)">Проверить</button>
  <p id="result"></p>
  <script>
    const options = [
      { text: "Хоу Ху, я получу тебя (в жёны)", correct: true },
      { text: "Хоу Ху, ты получишь меня", correct: false },
      { text: "Я отдам тебя Хоу Ху", correct: false },
      { text: "Хоу Ху, я не получу тебя", correct: false }
    ];

    function shuffle(array) {
      const arr = [...array];
      for (let i = arr.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [arr[i], arr[j]] = [arr[j], arr[i]];
      }
      return arr;
    }

    function renderOptions() {
      const shuffled = shuffle(options);
      const container = document.getElementById("answers");
      container.innerHTML = "";
      shuffled.forEach((opt, index) => {
        const label = document.createElement("label");
        const input = document.createElement("input");
        input.type = "radio";
        input.name = "q";
        input.value = opt.correct ? "1" : "0_" + index;
        input.dataset.correct = opt.correct.toString();
        label.appendChild(input);
        label.appendChild(document.createTextNode(opt.text));
        container.appendChild(label);
      });
    }

    function check(containerId, resultId, optionsArray) {
      const selected = document.querySelector('input[name="q"]:checked');
      const resultEl = document.getElementById(resultId);
      if (!selected) {
        resultEl.textContent = "Выберите вариант.";
        return;
      }
      if (selected.dataset.correct === "true") {
        resultEl.textContent = "✅ Правильно!";
        sendResult(true);
      } else {
        resultEl.textContent = "❌ Неверно.";
        sendResult(false);
      }
    }

    function sendResult(success) {
      window.parent.postMessage({ type: "TASK_RESULT", success }, "*");
    }

    window.onload = renderOptions;
  </script>
</body>
</html>
<script>window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");</script>

=== 📄 task11.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Задание: 帝其作王祸</title>
</head>
<body>
  <h2>Какой глагол требует двойного дополнения в древнекитайском?</h2>
  <p><strong>Пример: «帝其作王祸»</strong></p>
  <div class="pinyin-container">
    <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
    <div class="pinyin-text">Dì qí zuò wáng huò</div>
  </div>
  <div id="answers"></div>
  <button onclick="check('answers', 'result', options)">Проверить</button>
  <p id="result"></p>
  <script>
    const options = [
      { text: "作 (zuò) — причинять, создавать", correct: true },
      { text: "求 (qiú) — просить", correct: false },
      { text: "至 (zhì) — достигать", correct: false },
      { text: "卜 (bǔ) — гадать", correct: false }
    ];

    function shuffle(array) {
      const arr = [...array];
      for (let i = arr.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [arr[i], arr[j]] = [arr[j], arr[i]];
      }
      return arr;
    }

    function renderOptions() {
      const shuffled = shuffle(options);
      const container = document.getElementById("answers");
      container.innerHTML = "";
      shuffled.forEach((opt, index) => {
        const label = document.createElement("label");
        const input = document.createElement("input");
        input.type = "radio";
        input.name = "q";
        input.value = opt.correct ? "1" : "0_" + index;
        input.dataset.correct = opt.correct.toString();
        label.appendChild(input);
        label.appendChild(document.createTextNode(opt.text));
        container.appendChild(label);
      });
    }

    function check(containerId, resultId, optionsArray) {
      const selected = document.querySelector('input[name="q"]:checked');
      const resultEl = document.getElementById(resultId);
      if (!selected) {
        resultEl.textContent = "Выберите вариант.";
        return;
      }
      if (selected.dataset.correct === "true") {
        resultEl.textContent = "✅ Правильно!";
        sendResult(true);
      } else {
        resultEl.textContent = "❌ Неверно.";
        sendResult(false);
      }
    }

    function sendResult(success) {
      window.parent.postMessage({ type: "TASK_RESULT", success }, "*");
    }

    window.onload = renderOptions;
  </script>
</body>
</html>
<script>window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");</script>

=== 📄 task12.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Урок 5: Гадания о помощи Небесного Владыки</title>
</head>
<body>
<h2>Как правильно понять инверсию местоимения в отрицании?</h2>
<p><strong>Пример: «帝不我降熯»</strong></p>

<div class="pinyin-container">
    <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">
        Показать пиньинь
    </button>
    <div class="pinyin-text">dì bù wǒ jiàng hàn</div>
</div>

<p>Это предложение из гадательной надписи. Каково его правильное значение и грамматическое построение?</p>

<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>

<script>
  const options = [
    { text: "«Небесный Владыка не пошлёт нам засуху» — верно: инверсия местоимения «我» перед глаголом в отрицательном предложении", correct: true },
    { text: "«Небесный Владыка не пошлёт засуху им» — неверно: местоимение «我» относится к «ним», а не к «нам»", correct: false },
    { text: "«Небесный Владыка не мы пошлём засуху» — неверно: это бессмыслица, нарушена грамматика", correct: false },
    { text: "«Небесный Владыка не пошлёт засуху» — верно, но игнорируется роль местоимения «我»", correct: false }
  ];

  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }

  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) {
      console.error("Контейнер #answers не найден");
      return;
    }
    container.innerHTML = "";

    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = opt.correct ? "1" : "0_" + index;
      input.dataset.correct = opt.correct.toString();

      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }

  function check(containerId, resultId, optionsArray) {
    const selected = document.querySelector('input[name="q"]:checked');
    const resultEl = document.getElementById(resultId);

    if (!selected) {
      resultEl.textContent = "Выберите вариант.";
      return;
    }

    if (selected.dataset.correct === "true") {
      resultEl.textContent = "✅ Правильно! Это пример обязательной инверсии беспредложного местоимения «我» в отрицательном предложении: «帝不我降熯» = «Небесный Владыка не пошлёт нам засуху».";
      sendResult(true);
    } else {
      resultEl.textContent = "❌ Неверно. Вспомните правило: в отрицательных предложениях местоимение-дополнение ставится перед глаголом, сразу после отрицания.";
      sendResult(false);
    }
  }

  function sendResult(success) {
    window.parent.postMessage({ type: "TASK_RESULT", success }, "*");
  }

  // Загрузка вариантов при открытии страницы
  window.onload = renderOptions;
</script>
</body>
</html>

<!-- Уведомление, что задание существует -->
<script>window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");</script>

=== 📄 task13.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Урок 5: Божественная помощь и человеческие дела</title>
</head>
<body>
<h2>Как правильно понять предложение «帝不我其受佑»?</h2>
<p><strong>Пример из текста: «帝不我其受佑»</strong></p>

<div class="pinyin-container">
    <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">
        Показать пиньинь
    </button>
    <div class="pinyin-text">dì bù wǒ qí shòu yòu</div>
</div>

<p>Это гадательное предложение затрагивает несколько грамматических особенностей Урока 5. Каково его правильное значение и структура?</p>

<div id="answers"></div>
<button onclick="check('answers', 'result', options)">Проверить</button>
<p id="result"></p>

<script>
  const options = [
    { 
      text: "«Небесный Владыка не пошлёт нам помощь» — верно: здесь инверсия местоимения «我» перед глаголом в отрицании, а «受佑» — предикатив с двойным дополнением (кому? что?)", 
      correct: true 
    },
    { 
      text: "«Небесный Владыка не пошлёт помощь им» — неверно: «我» не может означать «их», это 1-е лицо", 
      correct: false 
    },
    { 
      text: "«Небесный Владыка не мы получим помощь» — неверно: нарушена синтаксическая структура", 
      correct: false 
    },
    { 
      text: "«Небесный Владыка не пошлёт помощь» — частично верно, но игнорирует грамматику: «我» не может стоять после «不» без инверсии", 
      correct: false 
    }
  ];

  function shuffle(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
  }

  function renderOptions() {
    const shuffled = shuffle(options);
    const container = document.getElementById("answers");
    if (!container) {
      console.error("Контейнер #answers не найден");
      return;
    }
    container.innerHTML = "";

    shuffled.forEach((opt, index) => {
      const label = document.createElement("label");
      const input = document.createElement("input");
      input.type = "radio";
      input.name = "q";
      input.value = opt.correct ? "1" : "0_" + index;
      input.dataset.correct = opt.correct.toString();

      label.appendChild(input);
      label.appendChild(document.createTextNode(opt.text));
      container.appendChild(label);
    });
  }

  function check(containerId, resultId, optionsArray) {
    const selected = document.querySelector('input[name="q"]:checked');
    const resultEl = document.getElementById(resultId);

    if (!selected) {
      resultEl.textContent = "Выберите вариант.";
      return;
    }

    if (selected.dataset.correct === "true") {
      resultEl.innerHTML = `
        ✅ <strong>Правильно!</strong><br>
        Это сложное предложение, объединяющее несколько тем Урока 5:<br>
        • <strong>Предикатив «受佑»</strong> требует двойного дополнения: «послать помощь кому?» (косвенное) и «что?» (прямое).<br>
        • <strong>Инверсия «不我受佑»</strong> — обязательна в отрицательных предложениях с местоимением-дополнением.<br>
        • Использование <strong>「我」</strong> как косвенного дополнения (реципиент) без предлога — норма для таких конструкций.<br><br>
        Полный перевод: «Небесный Владыка не пошлёт нам помощь».
      `;
      sendResult(true);
    } else {
      resultEl.textContent = "❌ Неверно. Вспомните: в отрицательных предложениях местоимение-дополнение к глаголу «дать/послать» ставится перед глаголом.";
      sendResult(false);
    }
  }

  function sendResult(success) {
    window.parent.postMessage({ type: "TASK_RESULT", success }, "*");
  }

  window.onload = renderOptions;
</script>
</body>
</html>

<!-- Уведомление, что задание существует -->
<script>window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");</script>

=== 📄 task14.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Урок 5 — Божественная помощь и человеческие судьбы</title>
</head>
<body>

<h2>Выберите правильное значение или объяснение</h2>

<div class="grid">
    <div class="cell" onclick="openModal('q1')">
        <strong>帝不我降熯</strong>
        <span class="status" id="status-q1">?</span>
    </div>
    <div class="cell" onclick="openModal('q2')">
        <strong>余其得汝</strong>
        <span class="status" id="status-q2">?</span>
    </div>
    <div class="cell" onclick="openModal('q3')">
        <strong>朕使</strong>
        <span class="status" id="status-q3">?</span>
    </div>
    <div class="cell" onclick="openModal('q4')">
        <strong>帝受我佑</strong>
        <span class="status" id="status-q4">?</span>
    </div>
</div>

<button onclick="checkAll()" style="width:100%; margin-top:12px;">Проверить</button>
<p id="result"></p>

<!-- Модальные окна -->
<div id="modal-q1" class="modal">
    <div class="modal-content">
        <div class="modal-header">帝不我降熯</div>
        <div class="pinyin-container">
            <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">
                Показать пиньинь
            </button>
            <div class="pinyin-text">dì bù wǒ jiàng hàn</div>
        </div>
        <div class="options">
            <label><input type="radio" name="q1" value="Небесный Владыка не пошлёт нам засуху">Небесный Владыка не пошлёт нам засуху</label>
            <label><input type="radio" name="q1" value="Небесный Владыка не мы пошлём засуху">Небесный Владыка не мы пошлём засуху</label>
            <label><input type="radio" name="q1" value="Небесный Владыка не пошлёт засуху им">Небесный Владыка не пошлёт засуху им</label>
            <label><input type="radio" name="q1" value="Небесный Владыка не пошлёт засуху">Небесный Владыка не пошлёт засуху</label>
        </div>
        <button onclick="closeModal('q1')">OK</button>
    </div>
</div>

<div id="modal-q2" class="modal">
    <div class="modal-content">
        <div class="modal-header">余其得汝</div>
        <div class="pinyin-container">
            <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">
                Показать пиньинь
            </button>
            <div class="pinyin-text">yú qí dé rǔ</div>
        </div>
        <div class="options">
            <label><input type="radio" name="q2" value="Я получу тебя (твою помощь)">Я получу тебя (твою помощь)</label>
            <label><input type="radio" name="q2" value="Я получу его">Я получу его</label>
            <label><input type="radio" name="q2" value="Мы получим вас">Мы получим вас</label>
            <label><input type="radio" name="q2" value="Я дам тебе">Я дам тебе</label>
        </div>
        <button onclick="closeModal('q2')">OK</button>
    </div>
</div>

<div id="modal-q3" class="modal">
    <div class="modal-content">
        <div class="modal-header">朕使</div>
        <div class="pinyin-container">
            <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">
                Показать пиньинь
            </button>
            <div class="pinyin-text">zhèn shǐ</div>
        </div>
        <div class="options">
            <label><input type="radio" name="q3" value="мой посланник">мой посланник</label>
            <label><input type="radio" name="q3" value="я пошлю">я пошлю</label>
            <label><input type="radio" name="q3" value="твой посланник">твой посланник</label>
            <label><input type="radio" name="q3" value="мы пошлём">мы пошлём</label>
        </div>
        <button onclick="closeModal('q3')">OK</button>
    </div>
</div>

<div id="modal-q4" class="modal">
    <div class="modal-content">
        <div class="modal-header">帝受我佑</div>
        <div class="pinyin-container">
            <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">
                Показать пиньинь
            </button>
            <div class="pinyin-text">dì shòu wǒ yòu</div>
        </div>
        <div class="options">
            <label><input type="radio" name="q4" value="Небесный Владыка пошлёт нам помощь">Небесный Владыка пошлёт нам помощь</label>
            <label><input type="radio" name="q4" value="Небесный Владыка получит нашу помощь">Небесный Владыка получит нашу помощь</label>
            <label><input type="radio" name="q4" value="Небесный Владыка не пошлёт помощи">Небесный Владыка не пошлёт помощи</label>
            <label><input type="radio" name="q4" value="Небесный Владыка поможет мне">Небесный Владыка поможет мне</label>
        </div>
        <button onclick="closeModal('q4')">OK</button>
    </div>
</div>

<script>
    const correctAnswers = {
        q1: "Небесный Владыка не пошлёт нам засуху",
        q2: "Я получу тебя (твою помощь)",
        q3: "мой посланник",
        q4: "Небесный Владыка пошлёт нам помощь"
    };

    let userAnswers = { q1: null, q2: null, q3: null, q4: null };

    function openModal(id) {
        document.getElementById(`modal-${id}`).style.display = "flex";
    }

    function closeModal(id) {
        const selected = document.querySelector(`input[name="${id}"]:checked`);
        if (selected) {
            userAnswers[id] = selected.value;
            document.getElementById(`status-${id}`).textContent = "✓";
            document.getElementById(`status-${id}`).style.color = "green";
        }
        document.getElementById(`modal-${id}`).style.display = "none";
    }

    function checkAll() {
        const resultEl = document.getElementById("result");
        const allAnswered = Object.values(userAnswers).every(ans => ans !== null);

        if (!allAnswered) {
            resultEl.textContent = "Ответьте на все.";
            return;
        }

        let correctCount = 0;
        for (const id in correctAnswers) {
            if (userAnswers[id] === correctAnswers[id]) correctCount++;
        }

        if (correctCount === 4) {
            resultEl.innerHTML = `
                ✅ <strong>Все верно!</strong><br>
                Вы отлично усвоили Урок 5:<br>
                • Инверсия местоимения в отрицании (不我降熯)<br>
                • Двойное дополнение (受佑)<br>
                • Функции местоимений (余 — подлежащее, 朕 — определение)<br>
                • Божественное вмешательство (帝)
            `;
            sendResult(true);
        } else {
            const wrong = 4 - correctCount;
            resultEl.textContent = `❌ ${wrong} ошибки.`;
            sendResult(false, `Ошибки: ${wrong}/4`);
        }
    }

    function sendResult(success, message) {
        window.parent.postMessage({ type: "TASK_RESULT", success, message }, "*");
    }

    // Уведомление, что задание существует
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task15.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Собери перевод: Урок 5</title>
</head>
<body>

<h2>Собери перевод: Урок 5</h2>

<div class="instructions">
    <p><strong>Кликайте по словам</strong>, чтобы собрать правильный перевод.</p>
    <p>Каждое задание фокусируется на ключевых грамматических структурах Урока 5.</p>
    <p>💡 <em>Обратите внимание на инверсию, двойное дополнение и функции местоимений!</em></p>
</div>

<div class="task-container">
    <div class="task-title">1. 帝不我降熯</div>
    <div class="oracle-text">帝不我降熯</div>
    <div id="word-pool1" class="word-pool"></div>
    <div id="drop-area1" class="drop-area"></div>
    <div class="instructions" style="font-size:12px; padding:4px; margin-top:4px">
        <p>💡 Здесь важно: <strong>инверсия «我»</strong> перед глаголом в отрицании</p>
    </div>
</div>

<div class="task-container">
    <div class="task-title">2. 帝受我佑</div>
    <div class="oracle-text">帝受我佑</div>
    <div id="word-pool2" class="word-pool"></div>
    <div id="drop-area2" class="drop-area"></div>
    <div class="instructions" style="font-size:12px; padding:4px; margin-top:4px">
        <p>💡 Обратите внимание: <strong>двойное дополнение</strong> — кому? что?</p>
    </div>
</div>

<div class="task-container">
    <div class="task-title">3. 余其得汝</div>
    <div class="oracle-text">余其得汝</div>
    <div id="word-pool3" class="word-pool"></div>
    <div id="drop-area3" class="drop-area"></div>
    <div class="instructions" style="font-size:12px; padding:4px; margin-top:4px">
        <p>💡 Помните: <strong>「余」</strong> — 1-е лицо, может быть подлежащим и дополнением</p>
    </div>
</div>

<div class="task-container">
    <div class="task-title">4. 朕使</div>
    <div class="oracle-text">朕使</div>
    <div id="word-pool4" class="word-pool"></div>
    <div id="drop-area4" class="drop-area"></div>
    <div class="instructions" style="font-size:12px; padding:4px; margin-top:4px">
        <p>💡 Важно: <strong>「朕」</strong> употребляется ТОЛЬКО как определение</p>
    </div>
</div>

<div class="controls">
    <button id="check-btn">Проверить</button>
    <button id="reset-btn">Сбросить</button>
</div>

<p id="result"></p>

<script>
// Ожидаемые правильные варианты
const expected1 = [
    ['Небесный Владыка', 'не', 'послал', 'нам', 'засуху'],
    ['Небесный Владыка', 'не', 'послал', 'засуху', 'нам'],
    ['не', 'послал', 'нам', 'засуху', 'Небесный Владыка']
];

const expected2 = [
    ['Небесный Владыка', 'пошлёт', 'нам', 'помощь'],
    ['Небесный Владыка', 'ниспошлёт', 'нам', 'помощь'],
    ['нам', 'помощь', 'пошлёт', 'Небесный Владыка']
];

const expected3 = [
    ['Я', 'получу', 'тебя'],
    ['ты', 'будешь', 'получен', 'мной'],
    ['Я', 'получу', 'твою', 'помощь']
];

const expected4 = [
    ['мой', 'посланник'],
    ['мой', 'емиссар'],
    ['посланник', 'мой']
];

// Слова для выбора (с лишними)
const words1 = ['Небесный Владыка', 'не', 'послал', 'нам', 'засуху', 'их', 'ему', 'он', 'послал', 'дождь', 'вчера'];
const words2 = ['Небесный Владыка', 'пошлёт', 'нам', 'помощь', 'им', 'урожай', 'власть', 'мир', 'помощь', 'послал', 'вчера'];
const words3 = ['Я', 'получу', 'тебя', 'его', 'вас', 'власть', 'помощь', 'мы', 'ты', 'он', 'получил', 'меня'];
const words4 = ['мой', 'посланник', 'твой', 'емиссар', 'он', 'ему', 'наш', 'они', 'гость', 'воин', 'слуга'];

// DOM-элементы
const wordPool1 = document.getElementById('word-pool1');
const wordPool2 = document.getElementById('word-pool2');
const wordPool3 = document.getElementById('word-pool3');
const wordPool4 = document.getElementById('word-pool4');
const dropArea1 = document.getElementById('drop-area1');
const dropArea2 = document.getElementById('drop-area2');
const dropArea3 = document.getElementById('drop-area3');
const dropArea4 = document.getElementById('drop-area4');
const checkBtn = document.getElementById('check-btn');
const resetBtn = document.getElementById('reset-btn');
const resultEl = document.getElementById('result');

function shuffleArray(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
}

function createWordElements(words, pool, dropArea) {
    const shuffledWords = shuffleArray(words);
    return shuffledWords.map(word => {
        const box = document.createElement('div');
        box.className = 'word-box';
        box.textContent = word;

        box.onclick = () => {
            box.remove();
            dropArea.appendChild(box);
            updatePlaceholder(dropArea);
        };

        return box;
    });
}

function updatePlaceholder(dropArea) {
    const hasWords = Array.from(dropArea.children).some(el => el.classList.contains('word-box'));
    dropArea.classList.toggle('has-words', hasWords);
}

let wordElements1 = [];
let wordElements2 = [];
let wordElements3 = [];
let wordElements4 = [];

function init() {
    // Очистка
    [wordPool1, wordPool2, wordPool3, wordPool4].forEach(p => p.innerHTML = '');
    [dropArea1, dropArea2, dropArea3, dropArea4].forEach(d => d.innerHTML = '');
    resultEl.style.display = 'none';

    // Создание элементов
    wordElements1 = createWordElements(words1, wordPool1, dropArea1);
    wordElements2 = createWordElements(words2, wordPool2, dropArea2);
    wordElements3 = createWordElements(words3, wordPool3, dropArea3);
    wordElements4 = createWordElements(words4, wordPool4, dropArea4);

    // Добавление в пулы
    wordElements1.forEach(el => wordPool1.appendChild(el));
    wordElements2.forEach(el => wordPool2.appendChild(el));
    wordElements3.forEach(el => wordPool3.appendChild(el));
    wordElements4.forEach(el => wordPool4.appendChild(el));

    // Обновление плейсхолдеров
    [dropArea1, dropArea2, dropArea3, dropArea4].forEach(updatePlaceholder);
}

// Возврат слов в пул при клике
[dropArea1, dropArea2, dropArea3, dropArea4].forEach((area, i) => {
    area.addEventListener('click', (e) => {
        if (e.target.classList.contains('word-box')) {
            const box = e.target;
            box.remove();
            [wordPool1, wordPool2, wordPool3, wordPool4][i].appendChild(box);
            updatePlaceholder(area);
        }
    });
});

function getWords(dropArea) {
    return Array.from(dropArea.children)
        .filter(el => el.classList.contains('word-box'))
        .map(el => el.textContent.trim());
}

function arraysEqual(a, b) {
    if (a.length !== b.length) return false;
    return a.every((val, i) => val === b[i]);
}

checkBtn.addEventListener('click', () => {
    const current1 = getWords(dropArea1);
    const current2 = getWords(dropArea2);
    const current3 = getWords(dropArea3);
    const current4 = getWords(dropArea4);

    const isCorrect1 = expected1.some(option => arraysEqual(current1, option));
    const isCorrect2 = expected2.some(option => arraysEqual(current2, option));
    const isCorrect3 = expected3.some(option => arraysEqual(current3, option));
    const isCorrect4 = expected4.some(option => arraysEqual(current4, option));

    if (isCorrect1 && isCorrect2 && isCorrect3 && isCorrect4) {
        resultEl.innerHTML = `
            ✅ <b>Идеально!</b> Вы усвоили ключевые темы Урока 5:<br>
            • <strong>Инверсия в отрицании</strong> (不我降熯)<br>
            • <strong>Двойное дополнение</strong> (受我佑)<br>
            • <strong>Функции местоимений</strong> (余, 朕)<br>
            • <strong>Божественное вмешательство</strong> (帝)
        `;
        resultEl.className = 'message success';
        resultEl.style.display = 'block';
        sendResult(true);
    } else {
        let message = '❌ Проверьте порядок слов и грамматические конструкции:';
        if (!isCorrect1) message += '<br>• Инверсия «我» в отрицании: «не пошлёт нам»';
        if (!isCorrect2) message += '<br>• Двойное дополнение: «нам помощь»';
        if (!isCorrect3) message += '<br>• Местоимение «余» — 1-е лицо, подлежащее';
        if (!isCorrect4) message += '<br>• «朕» — только определение, не подлежащее';

        resultEl.innerHTML = message;
        resultEl.className = 'message error';
        resultEl.style.display = 'block';
        sendResult(false, message);
    }
});

resetBtn.addEventListener('click', init);

function sendResult(success, message) {
    window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Верно" : "Ошибка")
    }, "*");
}

// Инициализация
window.onload = init;
window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
</script>
</body>
</html>

=== 📄 task16.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Инверсия vs Прямой порядок: Урок 5</title>
</head>
<body>

<h2>Инверсия vs Прямой порядок</h2>

<div class="instructions">
    <div class="instructions-header" onclick="toggleInstructions()">
        <strong>📌 Инструкция (нажми для развертывания)</strong>
        <span class="arrow">▼</span>
    </div>
    <div class="instructions-content" style="display:none">
        <p><strong>Инверсия дополнения в архаическом китайском</strong></p>
        <p>В архаическом древнекитайском языке беспредложные дополнения (особенно местоимения) могут стоять перед глаголом — это называется <strong>инверсией</strong>.</p>

        <p>🟢 <strong>Прямой порядок</strong> (зеленый):</p>
        <ul>
            <li>Дополнение стоит после глагола: «受我佑» — «послать нам помощь»</li>
            <li>Используется в утвердительных предложениях</li>
        </ul>

        <p>🔴 <strong>Инверсия</strong> (красный):</p>
        <ul>
            <li>Дополнение (чаще местоимение) стоит <strong>перед</strong> глаголом: «不我受佑» — «не пошлёт нам помощи»</li>
            <li>Обязательна в <strong>отрицательных предложениях</strong> с местоимением-дополнением</li>
            <li>Только с отрицанием <strong>不</strong>, а не 弗</li>
        </ul>

        <p><em>Примеры:</em><br>
        "帝受我佑" → 🟢 (прямой порядок)<br>
        "帝不我受佑" → 🔴 (инверсия)</p>

        <p><strong>Важно!</strong><br>
        Инверсия <strong>обязательна</strong>, если:
        <ol>
            <li>Предложение отрицательное</li>
            <li>Дополнение — местоимение</li>
            <li>Глагол требует двойного дополнения (受, 降, 作 и др.)</li>
        </ol>
        Нарушение этого правила — грамматическая ошибка.</p>
    </div>
</div>

<script>
function toggleInstructions() {
    const content = document.querySelector('.instructions-content');
    const header = document.querySelector('.instructions-header');
    const arrow = header.querySelector('.arrow');
    const isHidden = content.style.display === 'none';

    content.style.display = isHidden ? 'block' : 'none';
    arrow.textContent = isHidden ? '▲' : '▼';

    // Уведомление родителя о новой высоте
    setTimeout(() => {
        window.parent.postMessage({
            type: "UPDATE_HEIGHT",
            height: document.body.scrollHeight
        }, "*");
    }, 100);
}
</script>

<div class="columns">
    <div id="column1" class="column"></div>
    <div id="column2" class="column"></div>
</div>

<div class="controls">
    <button id="check-btn">Проверить</button>
    <button id="reset-btn">Сбросить</button>
</div>

<p id="result"></p>

<script>
const sentences = [
    { text: "帝受我佑", type: "direct", explanation: "Небесный Владыка пошлёт нам помощь — прямой порядок, утверждение" },
    { text: "帝不我受佑", type: "inversion", explanation: "Небесный Владыка не пошлёт нам помощи — инверсия обязательна в отрицании с местоимением" },
    { text: "帝降我熯", type: "direct", explanation: "Небесный Владыка пошлёт нам засуху — прямой порядок, утверждение" },
    { text: "帝不我降熯", type: "inversion", explanation: "Небесный Владыка не пошлёт нам засуху — инверсия обязательна" },
    { text: "洹作兹邑祸", type: "direct", explanation: "Река Хуань нанесёт несчастье этому городу — прямой порядок" },
    { text: "洹弗作兹邑祸", type: "direct", explanation: "Река Хуань не нанесёт несчастье этому городу — здесь 弗, но нет местоимения, инверсия не требуется" },
    { text: "帝不我其受佑", type: "inversion", explanation: "Небесный Владыка не пошлёт ли нам помощи? — инверсия сохраняется даже с частицей 其" },
    { text: "余其得汝", type: "direct", explanation: "Я получу тебя — прямой порядок, утверждение, нет отрицания" },
    { text: "余不其得汝", type: "inversion", explanation: "Я не получу ли тебя? — отрицание + местоимение → инверсия обязательна" },
    { text: "朕使", type: "direct", explanation: "мой посланник — здесь 朕 в функции определения, не дополнение" }
];

function shuffleArray(array) {
    const arr = [...array];
    for (let i = arr.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [arr[i], arr[j]] = [arr[j], arr[i]];
    }
    return arr;
}

function createSentenceElement(sentence, index) {
    const item = document.createElement('div');
    item.className = 'sentence-item';
    item.dataset.index = index;

    item.innerHTML = `
        <div class="oracle-text">${sentence.text}</div>
        <div class="toggle-container">
            <span class="toggle-text">прямой</span>
            <label class="toggle-switch">
                <input type="checkbox">
                <span class="slider"></span>
            </label>
            <span class="toggle-text">инверсия</span>
        </div>
    `;

    return item;
}

function init() {
    const shuffled = shuffleArray([...sentences]);
    const column1 = document.getElementById('column1');
    const column2 = document.getElementById('column2');

    column1.innerHTML = '';
    column2.innerHTML = '';
    document.getElementById('result').style.display = 'none';

    document.querySelectorAll('.sentence-item').forEach(el => el.remove());

    shuffled.slice(0, 5).forEach((s, i) => {
        const el = createSentenceElement(s, i);
        column1.appendChild(el);
    });

    shuffled.slice(5).forEach((s, i) => {
        const el = createSentenceElement(s, i + 5);
        column2.appendChild(el);
    });
}

function checkAnswers() {
    const items = document.querySelectorAll('.sentence-item');
    let allCorrect = true;

    items.forEach(item => {
        const index = item.dataset.index;
        const input = item.querySelector('input');
        const expectedType = sentences[index].type;
        const isCorrect = (input.checked && expectedType === "inversion") ||
                          (!input.checked && expectedType === "direct");

        item.classList.remove('correct', 'incorrect');
        if (isCorrect) {
            item.classList.add('correct');
        } else {
            item.classList.add('incorrect');
            allCorrect = false;
        }
    });

    const resultEl = document.getElementById('result');
    if (allCorrect) {
        resultEl.innerHTML = `
            ✅ <b>Отлично!</b> Вы правильно определили все случаи инверсии.<br>
            <small>Вы усвоили: инверсия обязательна в отрицаниях с местоимением, даже с частицей 其, и только с 不.</small>
        `;
        resultEl.className = 'success';
        sendResult(true);
    } else {
        resultEl.innerHTML = `❌ <b>Ошибка</b> в одном или нескольких определениях.`;
        resultEl.className = 'error';
        sendResult(false);
    }
    resultEl.style.display = 'block';
}

function reset() {
    document.querySelectorAll('.toggle-switch input').forEach(input => {
        input.checked = false;
    });
    document.querySelectorAll('.sentence-item').forEach(item => {
        item.classList.remove('correct', 'incorrect');
    });
    document.getElementById('result').style.display = 'none';
}

function sendResult(success) {
    window.parent.postMessage({
        type: "TASK_RESULT",
        success: success,
        message: success ? "Верно" : "Ошибка"
    }, "*");
}

// Инициализация
window.onload = init;
window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");

document.getElementById('check-btn').addEventListener('click', checkAnswers);
document.getElementById('reset-btn').addEventListener('click', reset);
</script>
</body>
</html>

=== 📄 task17.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Сопоставьте: Урок 5</title>
</head>
<body>
  <h2>Сопоставьте иероглиф и значение</h2>
  <p class="intro">Кликните на иероглиф слева, затем — на значение справа</p>
  
  <div class="match-container">
    <div class="column">
      <h3>Иероглифы</h3>
      <div id="words"></div>
    </div>
    <div class="column">
      <h3>Значения</h3>
      <div id="defs"></div>
    </div>
  </div>

  <button onclick="reset()">Сбросить</button>
  <p id="result"></p>

  <script>
    // Словарь Урока 5 — ключевые лексемы с грамматическими и культурными пояснениями
// Первая пятерка
const pairs1 = [
  { char: "帝", meaning: "Небесный Владыка — высшее божество, от которого зависят урожай, победа и природные явления" },
  { char: "熯", meaning: "иссушить, вызвать засуху — природное бедствие, противоположность дождю" },
  { char: "申", meaning: "заяц (земная ветвь); также может означать 'заявлять, объявлять'" },
  { char: "降", meaning: "послать (сверху вниз) — используется с божественными действиями, например, «послать помощь» или «послать засуху»" },
  { char: "伐", meaning: "нанести удар, напасть — военное действие, часто совершается с божественного разрешения" }
];

// Вторая пятерка
const pairs2 = [
  { char: "佑", meaning: "помощь, покровительство — обычно от Небесного Владыки; в глагольной конструкции «受佑» — «получить помощь»" },
  { char: "洹", meaning: "река Хуань — приток Хуанхэ, угрожавшая наводнениями столице Шан" },
  { char: "作", meaning: "нанести, причинить (вред) — часто с «祸»: «作祸» = «навредить, принести несчастье»" },
  { char: "茲", meaning: "этот, здесь — указательное местоимение для времени и пространства, синоним «今»" },
  { char: "禍", meaning: "бедствие, несчастье — может быть вызвано рекой, племенами или божеством" }
];

// Третья пятерка
const pairs3 = [
  { char: "余", meaning: "я (1-е лицо) — подлежащее или дополнение; может означать и 'мы'" },
  { char: "得", meaning: "получить, обрести — часто в значении 'удастся ли получить'" },
  { char: "汝", meaning: "ты (2-е лицо) — может быть подлежащим или дополнением" },
  { char: "使", meaning: "посланник, посол — существительное, производное от глагола 'посылать'" },
  { char: "合", meaning: "собрать, объединить (воинов) — военная мобилизация" }
];

// Четвёртая пятерка (неполная)
const pairs4 = [
  { char: "乃", meaning: "твой (2-е лицо, только в функции определения!) — например, «твой посланник»" },
  { char: "以", meaning: "привести, использовать (посланников) — глагол управления" },
  { char: "羌", meaning: "племя Цянь — одно из враждебных племён на западных границах Шан" },
  { char: "朕", meaning: "мой (только в функции определения!) — не может быть подлежащим, только «мой посланник», «моя помощь» и т.д." }
];

pairs = pairs1;

    let selectedWord = null;
    let matchedPairs = 0;

    function shuffle(arr) {
      return [...arr].sort(() => Math.random() - 0.5);
    }

    function init() {
      const wordList = document.getElementById("words");
      const defList = document.getElementById("defs");
      wordList.innerHTML = "";
      defList.innerHTML = "";
      const shuffledChars = shuffle(pairs);
      const shuffledMeanings = shuffle(pairs.map(p => p.meaning));

      // Создаём иероглифы (слева)
      shuffledChars.forEach(item => {
        const el = document.createElement("div");
        el.className = "word-item";
        el.dataset.char = item.char;
        el.textContent = item.char;
        el.onclick = () => selectWord(el);
        wordList.appendChild(el);
      });

      // Создаём значения (справа)
      shuffledMeanings.forEach(meaning => {
        const el = document.createElement("div");
        el.className = "def-item";
        el.dataset.meaning = meaning;
        el.textContent = meaning;
        el.onclick = () => selectDefinition(el);
        defList.appendChild(el);
      });
    }

    function selectWord(el) {
      if (el.classList.contains("matched")) return;
      document.querySelectorAll(".word-item.selected").forEach(e => e.classList.remove("selected"));
      selectedWord = null;

      el.classList.add("selected");
      selectedWord = el;
    }

    function selectDefinition(el) {
      if (el.classList.contains("matched") || !selectedWord) return;

      el.classList.add("selected");
      const selectedMeaning = el.dataset.meaning;
      const selectedChar = selectedWord.dataset.char;

      // Поиск соответствующей пары
      const correctPair = pairs.find(p => p.char === selectedChar);
      if (correctPair && correctPair.meaning === selectedMeaning) {
        // Верное сопоставление
        selectedWord.classList.add("matched");
        el.classList.add("matched");
        selectedWord.onclick = null;
        el.onclick = null;
        selectedWord = null;
        matchedPairs++;

        if (matchedPairs === 10) {
          const resultEl = document.getElementById("result");
          resultEl.innerHTML = `
            ✅ <strong>Отлично!</strong> Вы усвоили ключевую лексику Урока 5:<br>
            • Божественное вмешательство (<strong>帝</strong>)<br>
            • Глаголы «дать/нанести» (<strong>降</strong>, <strong>作</strong>)<br>
            • Местоимения с ограничениями (<strong>朕</strong>, <strong>乃</strong>)<br>
            • Природные явления (<strong>熯</strong>, <strong>禍</strong>)
          `;
          resultEl.className = "success";
          sendResult(true);
        }
      } else {
        // Ошибка
        document.getElementById("result").textContent = "❌ Ошибка.";
        setTimeout(() => {
          document.getElementById("result").textContent = "";
        }, 1500);
        selectedWord.classList.remove("selected");
        el.classList.remove("selected");
        selectedWord = null;
      }
    }

    function reset() {
      matchedPairs = 0;
      document.getElementById("result").textContent = "";
      document.getElementById("result").className = "";
      init();
    }

    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Все верно" : "Ошибка")
      }, "*");
    }

    window.onload = init;
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>


=== 📄 task18.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Сопоставьте: Урок 5</title>
</head>
<body>
  <h2>Сопоставьте иероглиф и значение</h2>
  <p class="intro">Кликните на иероглиф слева, затем — на значение справа</p>
  
  <div class="match-container">
    <div class="column">
      <h3>Иероглифы</h3>
      <div id="words"></div>
    </div>
    <div class="column">
      <h3>Значения</h3>
      <div id="defs"></div>
    </div>
  </div>

  <button onclick="reset()">Сбросить</button>
  <p id="result"></p>

  <script>
    // Словарь Урока 5 — ключевые лексемы с грамматическими и культурными пояснениями
// Первая пятерка
const pairs1 = [
  { char: "帝", meaning: "Небесный Владыка — высшее божество, от которого зависят урожай, победа и природные явления" },
  { char: "熯", meaning: "иссушить, вызвать засуху — природное бедствие, противоположность дождю" },
  { char: "申", meaning: "заяц (земная ветвь); также может означать 'заявлять, объявлять'" },
  { char: "降", meaning: "послать (сверху вниз) — используется с божественными действиями, например, «послать помощь» или «послать засуху»" },
  { char: "伐", meaning: "нанести удар, напасть — военное действие, часто совершается с божественного разрешения" }
];

// Вторая пятерка
const pairs2 = [
  { char: "佑", meaning: "помощь, покровительство — обычно от Небесного Владыки; в глагольной конструкции «受佑» — «получить помощь»" },
  { char: "洹", meaning: "река Хуань — приток Хуанхэ, угрожавшая наводнениями столице Шан" },
  { char: "作", meaning: "нанести, причинить (вред) — часто с «祸»: «作祸» = «навредить, принести несчастье»" },
  { char: "茲", meaning: "этот, здесь — указательное местоимение для времени и пространства, синоним «今»" },
  { char: "禍", meaning: "бедствие, несчастье — может быть вызвано рекой, племенами или божеством" }
];

// Третья пятерка
const pairs3 = [
  { char: "余", meaning: "я (1-е лицо) — подлежащее или дополнение; может означать и 'мы'" },
  { char: "得", meaning: "получить, обрести — часто в значении 'удастся ли получить'" },
  { char: "汝", meaning: "ты (2-е лицо) — может быть подлежащим или дополнением" },
  { char: "使", meaning: "посланник, посол — существительное, производное от глагола 'посылать'" },
  { char: "合", meaning: "собрать, объединить (воинов) — военная мобилизация" }
];

// Четвёртая пятерка (неполная)
const pairs4 = [
  { char: "乃", meaning: "твой (2-е лицо, только в функции определения!) — например, «твой посланник»" },
  { char: "以", meaning: "привести, использовать (посланников) — глагол управления" },
  { char: "羌", meaning: "племя Цянь — одно из враждебных племён на западных границах Шан" },
  { char: "朕", meaning: "мой (только в функции определения!) — не может быть подлежащим, только «мой посланник», «моя помощь» и т.д." }
];

pairs = pairs2;

    let selectedWord = null;
    let matchedPairs = 0;

    function shuffle(arr) {
      return [...arr].sort(() => Math.random() - 0.5);
    }

    function init() {
      const wordList = document.getElementById("words");
      const defList = document.getElementById("defs");
      wordList.innerHTML = "";
      defList.innerHTML = "";
      const shuffledChars = shuffle(pairs);
      const shuffledMeanings = shuffle(pairs.map(p => p.meaning));

      // Создаём иероглифы (слева)
      shuffledChars.forEach(item => {
        const el = document.createElement("div");
        el.className = "word-item";
        el.dataset.char = item.char;
        el.textContent = item.char;
        el.onclick = () => selectWord(el);
        wordList.appendChild(el);
      });

      // Создаём значения (справа)
      shuffledMeanings.forEach(meaning => {
        const el = document.createElement("div");
        el.className = "def-item";
        el.dataset.meaning = meaning;
        el.textContent = meaning;
        el.onclick = () => selectDefinition(el);
        defList.appendChild(el);
      });
    }

    function selectWord(el) {
      if (el.classList.contains("matched")) return;
      document.querySelectorAll(".word-item.selected").forEach(e => e.classList.remove("selected"));
      selectedWord = null;

      el.classList.add("selected");
      selectedWord = el;
    }

    function selectDefinition(el) {
      if (el.classList.contains("matched") || !selectedWord) return;

      el.classList.add("selected");
      const selectedMeaning = el.dataset.meaning;
      const selectedChar = selectedWord.dataset.char;

      // Поиск соответствующей пары
      const correctPair = pairs.find(p => p.char === selectedChar);
      if (correctPair && correctPair.meaning === selectedMeaning) {
        // Верное сопоставление
        selectedWord.classList.add("matched");
        el.classList.add("matched");
        selectedWord.onclick = null;
        el.onclick = null;
        selectedWord = null;
        matchedPairs++;

        if (matchedPairs === 10) {
          const resultEl = document.getElementById("result");
          resultEl.innerHTML = `
            ✅ <strong>Отлично!</strong> Вы усвоили ключевую лексику Урока 5:<br>
            • Божественное вмешательство (<strong>帝</strong>)<br>
            • Глаголы «дать/нанести» (<strong>降</strong>, <strong>作</strong>)<br>
            • Местоимения с ограничениями (<strong>朕</strong>, <strong>乃</strong>)<br>
            • Природные явления (<strong>熯</strong>, <strong>禍</strong>)
          `;
          resultEl.className = "success";
          sendResult(true);
        }
      } else {
        // Ошибка
        document.getElementById("result").textContent = "❌ Ошибка.";
        setTimeout(() => {
          document.getElementById("result").textContent = "";
        }, 1500);
        selectedWord.classList.remove("selected");
        el.classList.remove("selected");
        selectedWord = null;
      }
    }

    function reset() {
      matchedPairs = 0;
      document.getElementById("result").textContent = "";
      document.getElementById("result").className = "";
      init();
    }

    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Все верно" : "Ошибка")
      }, "*");
    }

    window.onload = init;
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>


=== 📄 task19.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Сопоставьте: Урок 5</title>
</head>
<body>
  <h2>Сопоставьте иероглиф и значение</h2>
  <p class="intro">Кликните на иероглиф слева, затем — на значение справа</p>
  
  <div class="match-container">
    <div class="column">
      <h3>Иероглифы</h3>
      <div id="words"></div>
    </div>
    <div class="column">
      <h3>Значения</h3>
      <div id="defs"></div>
    </div>
  </div>

  <button onclick="reset()">Сбросить</button>
  <p id="result"></p>

  <script>
    // Словарь Урока 5 — ключевые лексемы с грамматическими и культурными пояснениями
// Первая пятерка
const pairs1 = [
  { char: "帝", meaning: "Небесный Владыка — высшее божество, от которого зависят урожай, победа и природные явления" },
  { char: "熯", meaning: "иссушить, вызвать засуху — природное бедствие, противоположность дождю" },
  { char: "申", meaning: "заяц (земная ветвь); также может означать 'заявлять, объявлять'" },
  { char: "降", meaning: "послать (сверху вниз) — используется с божественными действиями, например, «послать помощь» или «послать засуху»" },
  { char: "伐", meaning: "нанести удар, напасть — военное действие, часто совершается с божественного разрешения" }
];

// Вторая пятерка
const pairs2 = [
  { char: "佑", meaning: "помощь, покровительство — обычно от Небесного Владыки; в глагольной конструкции «受佑» — «получить помощь»" },
  { char: "洹", meaning: "река Хуань — приток Хуанхэ, угрожавшая наводнениями столице Шан" },
  { char: "作", meaning: "нанести, причинить (вред) — часто с «祸»: «作祸» = «навредить, принести несчастье»" },
  { char: "茲", meaning: "этот, здесь — указательное местоимение для времени и пространства, синоним «今»" },
  { char: "禍", meaning: "бедствие, несчастье — может быть вызвано рекой, племенами или божеством" }
];

// Третья пятерка
const pairs3 = [
  { char: "余", meaning: "я (1-е лицо) — подлежащее или дополнение; может означать и 'мы'" },
  { char: "得", meaning: "получить, обрести — часто в значении 'удастся ли получить'" },
  { char: "汝", meaning: "ты (2-е лицо) — может быть подлежащим или дополнением" },
  { char: "使", meaning: "посланник, посол — существительное, производное от глагола 'посылать'" },
  { char: "合", meaning: "собрать, объединить (воинов) — военная мобилизация" }
];

// Четвёртая пятерка (неполная)
const pairs4 = [
  { char: "乃", meaning: "твой (2-е лицо, только в функции определения!) — например, «твой посланник»" },
  { char: "以", meaning: "привести, использовать (посланников) — глагол управления" },
  { char: "羌", meaning: "племя Цянь — одно из враждебных племён на западных границах Шан" },
  { char: "朕", meaning: "мой (только в функции определения!) — не может быть подлежащим, только «мой посланник», «моя помощь» и т.д." }
];

pairs = pairs3;

    let selectedWord = null;
    let matchedPairs = 0;

    function shuffle(arr) {
      return [...arr].sort(() => Math.random() - 0.5);
    }

    function init() {
      const wordList = document.getElementById("words");
      const defList = document.getElementById("defs");
      wordList.innerHTML = "";
      defList.innerHTML = "";
      const shuffledChars = shuffle(pairs);
      const shuffledMeanings = shuffle(pairs.map(p => p.meaning));

      // Создаём иероглифы (слева)
      shuffledChars.forEach(item => {
        const el = document.createElement("div");
        el.className = "word-item";
        el.dataset.char = item.char;
        el.textContent = item.char;
        el.onclick = () => selectWord(el);
        wordList.appendChild(el);
      });

      // Создаём значения (справа)
      shuffledMeanings.forEach(meaning => {
        const el = document.createElement("div");
        el.className = "def-item";
        el.dataset.meaning = meaning;
        el.textContent = meaning;
        el.onclick = () => selectDefinition(el);
        defList.appendChild(el);
      });
    }

    function selectWord(el) {
      if (el.classList.contains("matched")) return;
      document.querySelectorAll(".word-item.selected").forEach(e => e.classList.remove("selected"));
      selectedWord = null;

      el.classList.add("selected");
      selectedWord = el;
    }

    function selectDefinition(el) {
      if (el.classList.contains("matched") || !selectedWord) return;

      el.classList.add("selected");
      const selectedMeaning = el.dataset.meaning;
      const selectedChar = selectedWord.dataset.char;

      // Поиск соответствующей пары
      const correctPair = pairs.find(p => p.char === selectedChar);
      if (correctPair && correctPair.meaning === selectedMeaning) {
        // Верное сопоставление
        selectedWord.classList.add("matched");
        el.classList.add("matched");
        selectedWord.onclick = null;
        el.onclick = null;
        selectedWord = null;
        matchedPairs++;

        if (matchedPairs === 10) {
          const resultEl = document.getElementById("result");
          resultEl.innerHTML = `
            ✅ <strong>Отлично!</strong> Вы усвоили ключевую лексику Урока 5:<br>
            • Божественное вмешательство (<strong>帝</strong>)<br>
            • Глаголы «дать/нанести» (<strong>降</strong>, <strong>作</strong>)<br>
            • Местоимения с ограничениями (<strong>朕</strong>, <strong>乃</strong>)<br>
            • Природные явления (<strong>熯</strong>, <strong>禍</strong>)
          `;
          resultEl.className = "success";
          sendResult(true);
        }
      } else {
        // Ошибка
        document.getElementById("result").textContent = "❌ Ошибка.";
        setTimeout(() => {
          document.getElementById("result").textContent = "";
        }, 1500);
        selectedWord.classList.remove("selected");
        el.classList.remove("selected");
        selectedWord = null;
      }
    }

    function reset() {
      matchedPairs = 0;
      document.getElementById("result").textContent = "";
      document.getElementById("result").className = "";
      init();
    }

    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Все верно" : "Ошибка")
      }, "*");
    }

    window.onload = init;
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>


=== 📄 task2.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Собери фразу</title>
</head>
<body>
  <h2>Собери фразу</h2>
  <div class="instructions">
    <p><strong>Соберите фразу:</strong></p>
    <p><em>Небесный Владыка пошлет нам помощь.</em></p>
  </div>
  <!-- Кнопка для раскрытия подсказок -->
  <div class="toggle-hint" id="toggle-hint-btn">Показать подсказки</div>
  <!-- Контейнер подсказок (скрыт по умолчанию) -->
  <div class="hint-container" id="hint-container">
    <ul class="hint-list">
      <li>帝 — Небесный Владыка</li>
      <li>受 — даровать, пошлёт</li>
      <li>我 — нам</li>
      <li>佑 — помощь</li>
    </ul>
  </div>
  <p><strong>Иероглифы:</strong></p>
  <div id="char-pool"></div>
  <p><strong>Соберите здесь:</strong></p>
  <div id="drop-area"></div>
  <div class="controls">
    <button id="check-btn">Проверить</button>
    <button id="reset-btn">Сбросить</button>
  </div>
  <p id="result" class="message"></p>
  <script>
    const expected = ['帝', '受', '我', '佑'];
    const allChars = [
      '帝', '受', '我', '佑',
      '不', '其', '熯', '降', '伐', '作', '祸', '余', '汝', '乃', '以'
    ].sort(() => Math.random() - 0.5);
    const toggleBtn = document.getElementById('toggle-hint-btn');
    const hintContainer = document.getElementById('hint-container');
    const charPool = document.getElementById('char-pool');
    const dropArea = document.getElementById('drop-area');
    const checkBtn = document.getElementById('check-btn');
    const resetBtn = document.getElementById('reset-btn');
    const resultEl = document.getElementById('result');
    // Переключение подсказок
    toggleBtn.addEventListener('click', () => {
      const isOpen = hintContainer.classList.contains('open');
      hintContainer.classList.toggle('open');
      toggleBtn.textContent = isOpen ? 'Показать подсказки' : 'Скрыть подсказки';
    });
    function moveToDropArea(box) {
      const clone = box.cloneNode(true);
      clone.draggable = false;
      clone.style.cursor = 'default';
      clone.onclick = null;
      box.remove();
      dropArea.appendChild(clone);
    }
    function initCharPool() {
      charPool.innerHTML = '';
      dropArea.innerHTML = '';
      resultEl.textContent = '';
      allChars.forEach(char => {
        const box = document.createElement('div');
        box.className = 'char-box';
        box.textContent = char;
        box.addEventListener('click', () => moveToDropArea(box));
        charPool.appendChild(box);
      });
    }
    checkBtn.addEventListener('click', () => {
      const current = Array.from(dropArea.children).map(el => el.textContent);
      if (arraysEqual(current, expected)) {
        resultEl.innerHTML = '✅ Правильно!<br>帝受我佑';
        resultEl.className = 'message success';
        sendResult(true);
      } else {
        resultEl.textContent = '❌ Попробуйте снова.';
        resultEl.className = 'message';
        sendResult(false, 'Неверный порядок');
      }
    });
    resetBtn.addEventListener('click', initCharPool);
    function arraysEqual(a, b) {
      return a.length === b.length && a.every((v, i) => v === b[i]);
    }
    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Фраза собрана верно" : "Ошибка в порядке")
      }, "*");
    }
    // Инициализация
    initCharPool();
    // Сигнал, что задание загружено
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>
</html>

=== 📄 task20.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Сопоставьте: Урок 5</title>
</head>
<body>
  <h2>Сопоставьте иероглиф и значение</h2>
  <p class="intro">Кликните на иероглиф слева, затем — на значение справа</p>
  
  <div class="match-container">
    <div class="column">
      <h3>Иероглифы</h3>
      <div id="words"></div>
    </div>
    <div class="column">
      <h3>Значения</h3>
      <div id="defs"></div>
    </div>
  </div>

  <button onclick="reset()">Сбросить</button>
  <p id="result"></p>

  <script>
    // Словарь Урока 5 — ключевые лексемы с грамматическими и культурными пояснениями
// Первая пятерка
const pairs1 = [
  { char: "帝", meaning: "Небесный Владыка — высшее божество, от которого зависят урожай, победа и природные явления" },
  { char: "熯", meaning: "иссушить, вызвать засуху — природное бедствие, противоположность дождю" },
  { char: "申", meaning: "заяц (земная ветвь); также может означать 'заявлять, объявлять'" },
  { char: "降", meaning: "послать (сверху вниз) — используется с божественными действиями, например, «послать помощь» или «послать засуху»" },
  { char: "伐", meaning: "нанести удар, напасть — военное действие, часто совершается с божественного разрешения" }
];

// Вторая пятерка
const pairs2 = [
  { char: "佑", meaning: "помощь, покровительство — обычно от Небесного Владыки; в глагольной конструкции «受佑» — «получить помощь»" },
  { char: "洹", meaning: "река Хуань — приток Хуанхэ, угрожавшая наводнениями столице Шан" },
  { char: "作", meaning: "нанести, причинить (вред) — часто с «祸»: «作祸» = «навредить, принести несчастье»" },
  { char: "茲", meaning: "этот, здесь — указательное местоимение для времени и пространства, синоним «今»" },
  { char: "禍", meaning: "бедствие, несчастье — может быть вызвано рекой, племенами или божеством" }
];

// Третья пятерка
const pairs3 = [
  { char: "余", meaning: "я (1-е лицо) — подлежащее или дополнение; может означать и 'мы'" },
  { char: "得", meaning: "получить, обрести — часто в значении 'удастся ли получить'" },
  { char: "汝", meaning: "ты (2-е лицо) — может быть подлежащим или дополнением" },
  { char: "使", meaning: "посланник, посол — существительное, производное от глагола 'посылать'" },
  { char: "合", meaning: "собрать, объединить (воинов) — военная мобилизация" }
];

// Четвёртая пятерка (неполная)
const pairs4 = [
  { char: "乃", meaning: "твой (2-е лицо, только в функции определения!) — например, «твой посланник»" },
  { char: "以", meaning: "привести, использовать (посланников) — глагол управления" },
  { char: "羌", meaning: "племя Цянь — одно из враждебных племён на западных границах Шан" },
  { char: "朕", meaning: "мой (только в функции определения!) — не может быть подлежащим, только «мой посланник», «моя помощь» и т.д." }
];

pairs = pairs3;

    let selectedWord = null;
    let matchedPairs = 0;

    function shuffle(arr) {
      return [...arr].sort(() => Math.random() - 0.5);
    }

    function init() {
      const wordList = document.getElementById("words");
      const defList = document.getElementById("defs");
      wordList.innerHTML = "";
      defList.innerHTML = "";
      const shuffledChars = shuffle(pairs);
      const shuffledMeanings = shuffle(pairs.map(p => p.meaning));

      // Создаём иероглифы (слева)
      shuffledChars.forEach(item => {
        const el = document.createElement("div");
        el.className = "word-item";
        el.dataset.char = item.char;
        el.textContent = item.char;
        el.onclick = () => selectWord(el);
        wordList.appendChild(el);
      });

      // Создаём значения (справа)
      shuffledMeanings.forEach(meaning => {
        const el = document.createElement("div");
        el.className = "def-item";
        el.dataset.meaning = meaning;
        el.textContent = meaning;
        el.onclick = () => selectDefinition(el);
        defList.appendChild(el);
      });
    }

    function selectWord(el) {
      if (el.classList.contains("matched")) return;
      document.querySelectorAll(".word-item.selected").forEach(e => e.classList.remove("selected"));
      selectedWord = null;

      el.classList.add("selected");
      selectedWord = el;
    }

    function selectDefinition(el) {
      if (el.classList.contains("matched") || !selectedWord) return;

      el.classList.add("selected");
      const selectedMeaning = el.dataset.meaning;
      const selectedChar = selectedWord.dataset.char;

      // Поиск соответствующей пары
      const correctPair = pairs.find(p => p.char === selectedChar);
      if (correctPair && correctPair.meaning === selectedMeaning) {
        // Верное сопоставление
        selectedWord.classList.add("matched");
        el.classList.add("matched");
        selectedWord.onclick = null;
        el.onclick = null;
        selectedWord = null;
        matchedPairs++;

        if (matchedPairs === 10) {
          const resultEl = document.getElementById("result");
          resultEl.innerHTML = `
            ✅ <strong>Отлично!</strong> Вы усвоили ключевую лексику Урока 5:<br>
            • Божественное вмешательство (<strong>帝</strong>)<br>
            • Глаголы «дать/нанести» (<strong>降</strong>, <strong>作</strong>)<br>
            • Местоимения с ограничениями (<strong>朕</strong>, <strong>乃</strong>)<br>
            • Природные явления (<strong>熯</strong>, <strong>禍</strong>)
          `;
          resultEl.className = "success";
          sendResult(true);
        }
      } else {
        // Ошибка
        document.getElementById("result").textContent = "❌ Ошибка.";
        setTimeout(() => {
          document.getElementById("result").textContent = "";
        }, 1500);
        selectedWord.classList.remove("selected");
        el.classList.remove("selected");
        selectedWord = null;
      }
    }

    function reset() {
      matchedPairs = 0;
      document.getElementById("result").textContent = "";
      document.getElementById("result").className = "";
      init();
    }

    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Все верно" : "Ошибка")
      }, "*");
    }

    window.onload = init;
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>


=== 📄 task21.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Сопоставьте</title>
</head>
<body>
  <h2>Сопоставьте иероглиф и транскрипцию</h2>
  <p class="intro">Кликните на иероглиф слева, затем — на транскрипцию справа</p>
  <div class="match-container">
    <div class="column">
      <h3>Иероглифы</h3>
      <div id="words"></div>
    </div>
    <div class="column">
      <h3>Транскрипция</h3>
      <div id="defs"></div>
    </div>
  </div>
  <button onclick="reset()">Сбросить</button>
  <p id="result"></p>
  <script>
    // Словарь Урока 4 — только ключевые лексемы
const pairs1 = [
  { char: "帝", meaning: "dì" },
  { char: "熯", meaning: "hàn" },
  { char: "申", meaning: "shēn" },
  { char: "降", meaning: "jiàng" },
  { char: "伐", meaning: "fá" }
];

const pairs2 = [
  { char: "佑", meaning: "yòu" },
  { char: "洹", meaning: "huán" },
  { char: "作", meaning: "zuò" },
  { char: "茲", meaning: "zī" },
  { char: "禍", meaning: "huò" }
];

const pairs3 = [
  { char: "余", meaning: "yú" },
  { char: "得", meaning: "dé" },
  { char: "汝", meaning: "rǔ" },
  { char: "使", meaning: "shǐ" },
  { char: "合", meaning: "hé" }
];

const pairs4 = [
  { char: "乃", meaning: "nǎi" },
  { char: "以", meaning: "yǐ" },
  { char: "羌", meaning: "qiāng" },
  { char: "朕", meaning: "zhèn" }
];
pairs = pairs1
    let selectedWord = null;
    let matchedPairs = 0;

    function shuffle(arr) {
      return [...arr].sort(() => Math.random() - 0.5);
    }

    function init() {
      const wordList = document.getElementById("words");
      const defList = document.getElementById("defs");
      wordList.innerHTML = "";
      defList.innerHTML = "";
      const shuffledChars = shuffle(pairs);
      const shuffledMeanings = shuffle(pairs.map(p => p.meaning));

      // Создаём иероглифы (слева)
      shuffledChars.forEach(item => {
        const el = document.createElement("div");
        el.className = "word-item";
        el.dataset.char = item.char;
        el.textContent = item.char;
        el.onclick = () => selectWord(el);
        wordList.appendChild(el);
      });

      // Создаём значения (справа)
      shuffledMeanings.forEach(meaning => {
        const el = document.createElement("div");
        el.className = "def-item";
        el.dataset.meaning = meaning;
        el.textContent = meaning;
        el.onclick = () => selectDefinition(el);
        defList.appendChild(el);
      });
    }

    function selectWord(el) {
      if (el.classList.contains("matched")) return;
      document.querySelectorAll(".word-item.selected").forEach(e => e.classList.remove("selected"));
      selectedWord = null;

      el.classList.add("selected");
      selectedWord = el;
    }

    function selectDefinition(el) {
      if (el.classList.contains("matched") || !selectedWord) return;

      el.classList.add("selected");
      const selectedMeaning = el.dataset.meaning;
      const selectedChar = selectedWord.dataset.char;

      // Поиск соответствующей пары
      const correctPair = pairs.find(p => p.char === selectedChar);
      if (correctPair && correctPair.meaning === selectedMeaning) {
        // Верное сопоставление
        selectedWord.classList.add("matched");
        el.classList.add("matched");
        selectedWord.onclick = null;
        el.onclick = null;
        selectedWord = null;
        matchedPairs++;

        if (matchedPairs === 5) {
          const resultEl = document.getElementById("result");
          resultEl.textContent = "✅ Все пары верны!";
          resultEl.className = "success";
          sendResult(true);
        }
      } else {
        // Ошибка
        document.getElementById("result").textContent = "❌ Ошибка.";
        setTimeout(() => {
          document.getElementById("result").textContent = "";
        }, 1500);
        selectedWord.classList.remove("selected");
        el.classList.remove("selected");
        selectedWord = null;
      }
    }

    function reset() {
      matchedPairs = 0;
      document.getElementById("result").textContent = "";
      document.getElementById("result").className = "";
      init();
    }

    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Все верно" : "Ошибка")
      }, "*");
    }

    window.onload = init;
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>
</html>



=== 📄 task22.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Сопоставьте</title>
</head>
<body>
  <h2>Сопоставьте иероглиф и транскрипцию</h2>
  <p class="intro">Кликните на иероглиф слева, затем — на транскрипцию справа</p>
  <div class="match-container">
    <div class="column">
      <h3>Иероглифы</h3>
      <div id="words"></div>
    </div>
    <div class="column">
      <h3>Транскрипция</h3>
      <div id="defs"></div>
    </div>
  </div>
  <button onclick="reset()">Сбросить</button>
  <p id="result"></p>
  <script>
    // Словарь Урока 4 — только ключевые лексемы
const pairs1 = [
  { char: "帝", meaning: "dì" },
  { char: "熯", meaning: "hàn" },
  { char: "申", meaning: "shēn" },
  { char: "降", meaning: "jiàng" },
  { char: "伐", meaning: "fá" }
];

const pairs2 = [
  { char: "佑", meaning: "yòu" },
  { char: "洹", meaning: "huán" },
  { char: "作", meaning: "zuò" },
  { char: "茲", meaning: "zī" },
  { char: "禍", meaning: "huò" }
];

const pairs3 = [
  { char: "余", meaning: "yú" },
  { char: "得", meaning: "dé" },
  { char: "汝", meaning: "rǔ" },
  { char: "使", meaning: "shǐ" },
  { char: "合", meaning: "hé" }
];

const pairs4 = [
  { char: "乃", meaning: "nǎi" },
  { char: "以", meaning: "yǐ" },
  { char: "羌", meaning: "qiāng" },
  { char: "朕", meaning: "zhèn" }
];
pairs = pairs2
    let selectedWord = null;
    let matchedPairs = 0;

    function shuffle(arr) {
      return [...arr].sort(() => Math.random() - 0.5);
    }

    function init() {
      const wordList = document.getElementById("words");
      const defList = document.getElementById("defs");
      wordList.innerHTML = "";
      defList.innerHTML = "";
      const shuffledChars = shuffle(pairs);
      const shuffledMeanings = shuffle(pairs.map(p => p.meaning));

      // Создаём иероглифы (слева)
      shuffledChars.forEach(item => {
        const el = document.createElement("div");
        el.className = "word-item";
        el.dataset.char = item.char;
        el.textContent = item.char;
        el.onclick = () => selectWord(el);
        wordList.appendChild(el);
      });

      // Создаём значения (справа)
      shuffledMeanings.forEach(meaning => {
        const el = document.createElement("div");
        el.className = "def-item";
        el.dataset.meaning = meaning;
        el.textContent = meaning;
        el.onclick = () => selectDefinition(el);
        defList.appendChild(el);
      });
    }

    function selectWord(el) {
      if (el.classList.contains("matched")) return;
      document.querySelectorAll(".word-item.selected").forEach(e => e.classList.remove("selected"));
      selectedWord = null;

      el.classList.add("selected");
      selectedWord = el;
    }

    function selectDefinition(el) {
      if (el.classList.contains("matched") || !selectedWord) return;

      el.classList.add("selected");
      const selectedMeaning = el.dataset.meaning;
      const selectedChar = selectedWord.dataset.char;

      // Поиск соответствующей пары
      const correctPair = pairs.find(p => p.char === selectedChar);
      if (correctPair && correctPair.meaning === selectedMeaning) {
        // Верное сопоставление
        selectedWord.classList.add("matched");
        el.classList.add("matched");
        selectedWord.onclick = null;
        el.onclick = null;
        selectedWord = null;
        matchedPairs++;

        if (matchedPairs === 5) {
          const resultEl = document.getElementById("result");
          resultEl.textContent = "✅ Все пары верны!";
          resultEl.className = "success";
          sendResult(true);
        }
      } else {
        // Ошибка
        document.getElementById("result").textContent = "❌ Ошибка.";
        setTimeout(() => {
          document.getElementById("result").textContent = "";
        }, 1500);
        selectedWord.classList.remove("selected");
        el.classList.remove("selected");
        selectedWord = null;
      }
    }

    function reset() {
      matchedPairs = 0;
      document.getElementById("result").textContent = "";
      document.getElementById("result").className = "";
      init();
    }

    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Все верно" : "Ошибка")
      }, "*");
    }

    window.onload = init;
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>
</html>



=== 📄 task23.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Сопоставьте</title>
</head>
<body>
  <h2>Сопоставьте иероглиф и транскрипцию</h2>
  <p class="intro">Кликните на иероглиф слева, затем — на транскрипцию справа</p>
  <div class="match-container">
    <div class="column">
      <h3>Иероглифы</h3>
      <div id="words"></div>
    </div>
    <div class="column">
      <h3>Транскрипция</h3>
      <div id="defs"></div>
    </div>
  </div>
  <button onclick="reset()">Сбросить</button>
  <p id="result"></p>
  <script>
    // Словарь Урока 4 — только ключевые лексемы
const pairs1 = [
  { char: "帝", meaning: "dì" },
  { char: "熯", meaning: "hàn" },
  { char: "申", meaning: "shēn" },
  { char: "降", meaning: "jiàng" },
  { char: "伐", meaning: "fá" }
];

const pairs2 = [
  { char: "佑", meaning: "yòu" },
  { char: "洹", meaning: "huán" },
  { char: "作", meaning: "zuò" },
  { char: "茲", meaning: "zī" },
  { char: "禍", meaning: "huò" }
];

const pairs3 = [
  { char: "余", meaning: "yú" },
  { char: "得", meaning: "dé" },
  { char: "汝", meaning: "rǔ" },
  { char: "使", meaning: "shǐ" },
  { char: "合", meaning: "hé" }
];

const pairs4 = [
  { char: "乃", meaning: "nǎi" },
  { char: "以", meaning: "yǐ" },
  { char: "羌", meaning: "qiāng" },
  { char: "朕", meaning: "zhèn" }
];
pairs = pairs3
    let selectedWord = null;
    let matchedPairs = 0;

    function shuffle(arr) {
      return [...arr].sort(() => Math.random() - 0.5);
    }

    function init() {
      const wordList = document.getElementById("words");
      const defList = document.getElementById("defs");
      wordList.innerHTML = "";
      defList.innerHTML = "";
      const shuffledChars = shuffle(pairs);
      const shuffledMeanings = shuffle(pairs.map(p => p.meaning));

      // Создаём иероглифы (слева)
      shuffledChars.forEach(item => {
        const el = document.createElement("div");
        el.className = "word-item";
        el.dataset.char = item.char;
        el.textContent = item.char;
        el.onclick = () => selectWord(el);
        wordList.appendChild(el);
      });

      // Создаём значения (справа)
      shuffledMeanings.forEach(meaning => {
        const el = document.createElement("div");
        el.className = "def-item";
        el.dataset.meaning = meaning;
        el.textContent = meaning;
        el.onclick = () => selectDefinition(el);
        defList.appendChild(el);
      });
    }

    function selectWord(el) {
      if (el.classList.contains("matched")) return;
      document.querySelectorAll(".word-item.selected").forEach(e => e.classList.remove("selected"));
      selectedWord = null;

      el.classList.add("selected");
      selectedWord = el;
    }

    function selectDefinition(el) {
      if (el.classList.contains("matched") || !selectedWord) return;

      el.classList.add("selected");
      const selectedMeaning = el.dataset.meaning;
      const selectedChar = selectedWord.dataset.char;

      // Поиск соответствующей пары
      const correctPair = pairs.find(p => p.char === selectedChar);
      if (correctPair && correctPair.meaning === selectedMeaning) {
        // Верное сопоставление
        selectedWord.classList.add("matched");
        el.classList.add("matched");
        selectedWord.onclick = null;
        el.onclick = null;
        selectedWord = null;
        matchedPairs++;

        if (matchedPairs === 5) {
          const resultEl = document.getElementById("result");
          resultEl.textContent = "✅ Все пары верны!";
          resultEl.className = "success";
          sendResult(true);
        }
      } else {
        // Ошибка
        document.getElementById("result").textContent = "❌ Ошибка.";
        setTimeout(() => {
          document.getElementById("result").textContent = "";
        }, 1500);
        selectedWord.classList.remove("selected");
        el.classList.remove("selected");
        selectedWord = null;
      }
    }

    function reset() {
      matchedPairs = 0;
      document.getElementById("result").textContent = "";
      document.getElementById("result").className = "";
      init();
    }

    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Все верно" : "Ошибка")
      }, "*");
    }

    window.onload = init;
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>
</html>



=== 📄 task24.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Сопоставьте</title>
</head>
<body>
  <h2>Сопоставьте иероглиф и транскрипцию</h2>
  <p class="intro">Кликните на иероглиф слева, затем — на транскрипцию справа</p>
  <div class="match-container">
    <div class="column">
      <h3>Иероглифы</h3>
      <div id="words"></div>
    </div>
    <div class="column">
      <h3>Транскрипция</h3>
      <div id="defs"></div>
    </div>
  </div>
  <button onclick="reset()">Сбросить</button>
  <p id="result"></p>
  <script>
    // Словарь Урока 4 — только ключевые лексемы
const pairs1 = [
  { char: "帝", meaning: "dì" },
  { char: "熯", meaning: "hàn" },
  { char: "申", meaning: "shēn" },
  { char: "降", meaning: "jiàng" },
  { char: "伐", meaning: "fá" }
];

const pairs2 = [
  { char: "佑", meaning: "yòu" },
  { char: "洹", meaning: "huán" },
  { char: "作", meaning: "zuò" },
  { char: "茲", meaning: "zī" },
  { char: "禍", meaning: "huò" }
];

const pairs3 = [
  { char: "余", meaning: "yú" },
  { char: "得", meaning: "dé" },
  { char: "汝", meaning: "rǔ" },
  { char: "使", meaning: "shǐ" },
  { char: "合", meaning: "hé" }
];

const pairs4 = [
  { char: "乃", meaning: "nǎi" },
  { char: "以", meaning: "yǐ" },
  { char: "羌", meaning: "qiāng" },
  { char: "朕", meaning: "zhèn" }
];
pairs = pairs4
    let selectedWord = null;
    let matchedPairs = 0;

    function shuffle(arr) {
      return [...arr].sort(() => Math.random() - 0.5);
    }

    function init() {
      const wordList = document.getElementById("words");
      const defList = document.getElementById("defs");
      wordList.innerHTML = "";
      defList.innerHTML = "";
      const shuffledChars = shuffle(pairs);
      const shuffledMeanings = shuffle(pairs.map(p => p.meaning));

      // Создаём иероглифы (слева)
      shuffledChars.forEach(item => {
        const el = document.createElement("div");
        el.className = "word-item";
        el.dataset.char = item.char;
        el.textContent = item.char;
        el.onclick = () => selectWord(el);
        wordList.appendChild(el);
      });

      // Создаём значения (справа)
      shuffledMeanings.forEach(meaning => {
        const el = document.createElement("div");
        el.className = "def-item";
        el.dataset.meaning = meaning;
        el.textContent = meaning;
        el.onclick = () => selectDefinition(el);
        defList.appendChild(el);
      });
    }

    function selectWord(el) {
      if (el.classList.contains("matched")) return;
      document.querySelectorAll(".word-item.selected").forEach(e => e.classList.remove("selected"));
      selectedWord = null;

      el.classList.add("selected");
      selectedWord = el;
    }

    function selectDefinition(el) {
      if (el.classList.contains("matched") || !selectedWord) return;

      el.classList.add("selected");
      const selectedMeaning = el.dataset.meaning;
      const selectedChar = selectedWord.dataset.char;

      // Поиск соответствующей пары
      const correctPair = pairs.find(p => p.char === selectedChar);
      if (correctPair && correctPair.meaning === selectedMeaning) {
        // Верное сопоставление
        selectedWord.classList.add("matched");
        el.classList.add("matched");
        selectedWord.onclick = null;
        el.onclick = null;
        selectedWord = null;
        matchedPairs++;

        if (matchedPairs === 5) {
          const resultEl = document.getElementById("result");
          resultEl.textContent = "✅ Все пары верны!";
          resultEl.className = "success";
          sendResult(true);
        }
      } else {
        // Ошибка
        document.getElementById("result").textContent = "❌ Ошибка.";
        setTimeout(() => {
          document.getElementById("result").textContent = "";
        }, 1500);
        selectedWord.classList.remove("selected");
        el.classList.remove("selected");
        selectedWord = null;
      }
    }

    function reset() {
      matchedPairs = 0;
      document.getElementById("result").textContent = "";
      document.getElementById("result").className = "";
      init();
    }

    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Все верно" : "Ошибка")
      }, "*");
    }

    window.onload = init;
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>
</html>



=== 📄 task3.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Собери фразу</title>
</head>
<body>
  <h2>Собери фразу</h2>
  <div class="instructions">
    <p><strong>Соберите фразу:</strong></p>
    <p><em>Я получу помощь от племени Ма.</em></p>
  </div>
  <!-- Кнопка для раскрытия подсказок -->
  <div class="toggle-hint" id="toggle-hint-btn">Показать подсказки</div>
  <!-- Контейнер подсказок (скрыт по умолчанию) -->
  <div class="hint-container" id="hint-container">
    <ul class="hint-list">
      <li>余 — я</li>
      <li>受 — получу, пошлют</li>
      <li>馬方 — племя Ма</li>
      <li>佑 — помощь</li>
    </ul>
  </div>
  <p><strong>Иероглифы:</strong></p>
  <div id="char-pool"></div>
  <p><strong>Соберите здесь:</strong></p>
  <div id="drop-area"></div>
  <div class="controls">
    <button id="check-btn">Проверить</button>
    <button id="reset-btn">Сбросить</button>
  </div>
  <p id="result" class="message"></p>
  <script>
    const expected = ['余', '受', '馬', '方', '佑'];
    const allChars = [
      '余', '受', '馬', '方', '佑',
      '帝', '我', '不', '其', '作', '祸', '以', '乃', '伐', '得'
    ].sort(() => Math.random() - 0.5);
    const toggleBtn = document.getElementById('toggle-hint-btn');
    const hintContainer = document.getElementById('hint-container');
    const charPool = document.getElementById('char-pool');
    const dropArea = document.getElementById('drop-area');
    const checkBtn = document.getElementById('check-btn');
    const resetBtn = document.getElementById('reset-btn');
    const resultEl = document.getElementById('result');
    // Переключение подсказок
    toggleBtn.addEventListener('click', () => {
      const isOpen = hintContainer.classList.contains('open');
      hintContainer.classList.toggle('open');
      toggleBtn.textContent = isOpen ? 'Показать подсказки' : 'Скрыть подсказки';
    });
    function moveToDropArea(box) {
      const clone = box.cloneNode(true);
      clone.draggable = false;
      clone.style.cursor = 'default';
      clone.onclick = null;
      box.remove();
      dropArea.appendChild(clone);
    }
    function initCharPool() {
      charPool.innerHTML = '';
      dropArea.innerHTML = '';
      resultEl.textContent = '';
      allChars.forEach(char => {
        const box = document.createElement('div');
        box.className = 'char-box';
        box.textContent = char;
        box.addEventListener('click', () => moveToDropArea(box));
        charPool.appendChild(box);
      });
    }
    checkBtn.addEventListener('click', () => {
      const current = Array.from(dropArea.children).map(el => el.textContent);
      if (arraysEqual(current, expected)) {
        resultEl.innerHTML = '✅ Правильно!<br>余受馬方佑';
        resultEl.className = 'message success';
        sendResult(true);
      } else {
        resultEl.textContent = '❌ Попробуйте снова.';
        resultEl.className = 'message';
        sendResult(false, 'Неверный порядок');
      }
    });
    resetBtn.addEventListener('click', initCharPool);
    function arraysEqual(a, b) {
      return a.length === b.length && a.every((v, i) => v === b[i]);
    }
    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Фраза собрана верно" : "Ошибка в порядке")
      }, "*");
    }
    // Инициализация
    initCharPool();
    // Сигнал, что задание загружено
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>
</html>

=== 📄 task4.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Собери фразу</title>
</head>
<body>
  <h2>Собери фразу</h2>
  <div class="instructions">
    <p><strong>Соберите фразу:</strong></p>
    <p><em>Правитель не пошлет мне помощь.</em></p>
  </div>
  <!-- Кнопка для раскрытия подсказок -->
  <div class="toggle-hint" id="toggle-hint-btn">Показать подсказки</div>
  <!-- Контейнер подсказок (скрыт по умолчанию) -->
  <div class="hint-container" id="hint-container">
    <ul class="hint-list">
      <li>王 — правитель</li>
      <li>不 — не</li>
      <li>我 — мне</li>
      <li>受 — пошлёт</li>
      <li>佑 — помощь</li>
    </ul>
  </div>
  <p><strong>Иероглифы:</strong></p>
  <div id="char-pool"></div>
  <p><strong>Соберите здесь:</strong></p>
  <div id="drop-area"></div>
  <div class="controls">
    <button id="check-btn">Проверить</button>
    <button id="reset-btn">Сбросить</button>
  </div>
  <p id="result" class="message"></p>
  <script>
    const expected = ['王', '不', '我', '受', '佑'];
    const allChars = [
      '王', '不', '我', '受', '佑',
      '帝', '其', '作', '祸', '余', '以', '乃', '伐', '得', '汝'
    ].sort(() => Math.random() - 0.5);
    const toggleBtn = document.getElementById('toggle-hint-btn');
    const hintContainer = document.getElementById('hint-container');
    const charPool = document.getElementById('char-pool');
    const dropArea = document.getElementById('drop-area');
    const checkBtn = document.getElementById('check-btn');
    const resetBtn = document.getElementById('reset-btn');
    const resultEl = document.getElementById('result');
    // Переключение подсказок
    toggleBtn.addEventListener('click', () => {
      const isOpen = hintContainer.classList.contains('open');
      hintContainer.classList.toggle('open');
      toggleBtn.textContent = isOpen ? 'Показать подсказки' : 'Скрыть подсказки';
    });
    function moveToDropArea(box) {
      const clone = box.cloneNode(true);
      clone.draggable = false;
      clone.style.cursor = 'default';
      clone.onclick = null;
      box.remove();
      dropArea.appendChild(clone);
    }
    function initCharPool() {
      charPool.innerHTML = '';
      dropArea.innerHTML = '';
      resultEl.textContent = '';
      allChars.forEach(char => {
        const box = document.createElement('div');
        box.className = 'char-box';
        box.textContent = char;
        box.addEventListener('click', () => moveToDropArea(box));
        charPool.appendChild(box);
      });
    }
    checkBtn.addEventListener('click', () => {
      const current = Array.from(dropArea.children).map(el => el.textContent);
      if (arraysEqual(current, expected)) {
        resultEl.innerHTML = '✅ Правильно!<br>王不我受佑';
        resultEl.className = 'message success';
        sendResult(true);
      } else {
        resultEl.textContent = '❌ Попробуйте снова.';
        resultEl.className = 'message';
        sendResult(false, 'Неверный порядок');
      }
    });
    resetBtn.addEventListener('click', initCharPool);
    function arraysEqual(a, b) {
      return a.length === b.length && a.every((v, i) => v === b[i]);
    }
    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Фраза собрана верно" : "Ошибка в порядке")
      }, "*");
    }
    // Инициализация
    initCharPool();
    // Сигнал, что задание загружено
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>
</html>

=== 📄 task5.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Собери фразу</title>
</head>
<body>
  <h2>Собери фразу</h2>
  <div class="instructions">
    <p><strong>Соберите фразу:</strong></p>
    <p><em>Цяны не нанесут поражение моему послу.</em></p>
  </div>
  <!-- Кнопка для раскрытия подсказок -->
  <div class="toggle-hint" id="toggle-hint-btn">Показать подсказки</div>
  <!-- Контейнер подсказок (скрыт по умолчанию) -->
  <div class="hint-container" id="hint-container">
    <ul class="hint-list">
      <li>羌 — цяны</li>
      <li>弗 — не</li>
      <li>朕 — мой (притяжательное)</li>
      <li>使 — посол</li>
      <li>烖 — нанесут поражение</li>
    </ul>
  </div>
  <p><strong>Иероглифы:</strong></p>
  <div id="char-pool"></div>
  <p><strong>Соберите здесь:</strong></p>
  <div id="drop-area"></div>
  <div class="controls">
    <button id="check-btn">Проверить</button>
    <button id="reset-btn">Сбросить</button>
  </div>
  <p id="result" class="message"></p>
  <script>
    const expected = ['羌', '弗', '朕', '使', '烖'];
    const allChars = [
      '羌', '弗', '朕', '使', '烖',
      '帝', '我', '不', '其', '作', '祸', '余', '以', '乃', '得'
    ].sort(() => Math.random() - 0.5);
    const toggleBtn = document.getElementById('toggle-hint-btn');
    const hintContainer = document.getElementById('hint-container');
    const charPool = document.getElementById('char-pool');
    const dropArea = document.getElementById('drop-area');
    const checkBtn = document.getElementById('check-btn');
    const resetBtn = document.getElementById('reset-btn');
    const resultEl = document.getElementById('result');
    // Переключение подсказок
    toggleBtn.addEventListener('click', () => {
      const isOpen = hintContainer.classList.contains('open');
      hintContainer.classList.toggle('open');
      toggleBtn.textContent = isOpen ? 'Показать подсказки' : 'Скрыть подсказки';
    });
    function moveToDropArea(box) {
      const clone = box.cloneNode(true);
      clone.draggable = false;
      clone.style.cursor = 'default';
      clone.onclick = null;
      box.remove();
      dropArea.appendChild(clone);
    }
    function initCharPool() {
      charPool.innerHTML = '';
      dropArea.innerHTML = '';
      resultEl.textContent = '';
      allChars.forEach(char => {
        const box = document.createElement('div');
        box.className = 'char-box';
        box.textContent = char;
        box.addEventListener('click', () => moveToDropArea(box));
        charPool.appendChild(box);
      });
    }
    checkBtn.addEventListener('click', () => {
      const current = Array.from(dropArea.children).map(el => el.textContent);
      if (arraysEqual(current, expected)) {
        resultEl.innerHTML = '✅ Правильно!<br>羌弗朕使烖';
        resultEl.className = 'message success';
        sendResult(true);
      } else {
        resultEl.textContent = '❌ Попробуйте снова.';
        resultEl.className = 'message';
        sendResult(false, 'Неверный порядок');
      }
    });
    resetBtn.addEventListener('click', initCharPool);
    function arraysEqual(a, b) {
      return a.length === b.length && a.every((v, i) => v === b[i]);
    }
    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Фраза собрана верно" : "Ошибка в порядке")
      }, "*");
    }
    // Инициализация
    initCharPool();
    // Сигнал, что задание загружено
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>
</html>

=== 📄 task6.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Собери фразу</title>
</head>
<body>
  <h2>Собери фразу</h2>
  <div class="instructions">
    <p><strong>Соберите фразу:</strong></p>
    <p><em>Хоу Ху, я получу тебя (в жёны).</em></p>
  </div>
  <!-- Кнопка для раскрытия подсказок -->
  <div class="toggle-hint" id="toggle-hint-btn">Показать подсказки</div>
  <!-- Контейнер подсказок (скрыт по умолчанию) -->
  <div class="hint-container" id="hint-container">
    <ul class="hint-list">
      <li>侯虎 — Хоу Ху</li>
      <li>余 — я</li>
      <li>其 — будет</li>
      <li>得 — получить (в жёны)</li>
      <li>汝 — тебя</li>
    </ul>
  </div>
  <p><strong>Иероглифы:</strong></p>
  <div id="char-pool"></div>
  <p><strong>Соберите здесь:</strong></p>
  <div id="drop-area"></div>
  <div class="controls">
    <button id="check-btn">Проверить</button>
    <button id="reset-btn">Сбросить</button>
  </div>
  <p id="result" class="message"></p>
  <script>
    const expected = ['侯', '虎', '余', '其', '得', '汝'];
    const allChars = [
      '侯', '虎', '余', '其', '得', '汝',
      '帝', '我', '不', '受', '佑', '以', '乃', '使', '伐'
    ].sort(() => Math.random() - 0.5);
    const toggleBtn = document.getElementById('toggle-hint-btn');
    const hintContainer = document.getElementById('hint-container');
    const charPool = document.getElementById('char-pool');
    const dropArea = document.getElementById('drop-area');
    const checkBtn = document.getElementById('check-btn');
    const resetBtn = document.getElementById('reset-btn');
    const resultEl = document.getElementById('result');
    // Переключение подсказок
    toggleBtn.addEventListener('click', () => {
      const isOpen = hintContainer.classList.contains('open');
      hintContainer.classList.toggle('open');
      toggleBtn.textContent = isOpen ? 'Показать подсказки' : 'Скрыть подсказки';
    });
    function moveToDropArea(box) {
      const clone = box.cloneNode(true);
      clone.draggable = false;
      clone.style.cursor = 'default';
      clone.onclick = null;
      box.remove();
      dropArea.appendChild(clone);
    }
    function initCharPool() {
      charPool.innerHTML = '';
      dropArea.innerHTML = '';
      resultEl.textContent = '';
      allChars.forEach(char => {
        const box = document.createElement('div');
        box.className = 'char-box';
        box.textContent = char;
        box.addEventListener('click', () => moveToDropArea(box));
        charPool.appendChild(box);
      });
    }
    checkBtn.addEventListener('click', () => {
      const current = Array.from(dropArea.children).map(el => el.textContent);
      if (arraysEqual(current, expected)) {
        resultEl.innerHTML = '✅ Правильно!<br>侯虎余其得汝';
        resultEl.className = 'message success';
        sendResult(true);
      } else {
        resultEl.textContent = '❌ Попробуйте снова.';
        resultEl.className = 'message';
        sendResult(false, 'Неверный порядок');
      }
    });
    resetBtn.addEventListener('click', initCharPool);
    function arraysEqual(a, b) {
      return a.length === b.length && a.every((v, i) => v === b[i]);
    }
    function sendResult(success, message) {
      window.parent.postMessage({
        type: "TASK_RESULT",
        success: !!success,
        message: message || (success ? "Фраза собрана верно" : "Ошибка в порядке")
      }, "*");
    }
    // Инициализация
    initCharPool();
    // Сигнал, что задание загружено
    window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");
  </script>
</body>
</html>

=== 📄 task7.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Задание: 帝受我佑</title>
</head>
<body>
  <h2>Какой из вариантов является правильным переводом фразы «帝受我佑»?</h2>
  <p><strong>Пример: «帝受我佑»</strong></p>
  <div class="pinyin-container">
    <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
    <div class="pinyin-text">Dì shòu wǒ yòu</div>
  </div>
  <div id="answers"></div>
  <button onclick="check('answers', 'result', options)">Проверить</button>
  <p id="result"></p>
  <script>
    const options = [
      { text: "Небесный Владыка получит мою помощь", correct: false },
      { text: "Небесный Владыка пошлёт мне помощь", correct: true },
      { text: "Я получу помощь от Небесного Владыки", correct: false },
      { text: "Небесный Владыка не пошлёт помощи", correct: false }
    ];

    function shuffle(array) {
      const arr = [...array];
      for (let i = arr.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [arr[i], arr[j]] = [arr[j], arr[i]];
      }
      return arr;
    }

    function renderOptions() {
      const shuffled = shuffle(options);
      const container = document.getElementById("answers");
      container.innerHTML = "";
      shuffled.forEach((opt, index) => {
        const label = document.createElement("label");
        const input = document.createElement("input");
        input.type = "radio";
        input.name = "q";
        input.value = opt.correct ? "1" : "0_" + index;
        input.dataset.correct = opt.correct.toString();
        label.appendChild(input);
        label.appendChild(document.createTextNode(opt.text));
        container.appendChild(label);
      });
    }

    function check(containerId, resultId, optionsArray) {
      const selected = document.querySelector('input[name="q"]:checked');
      const resultEl = document.getElementById(resultId);
      if (!selected) {
        resultEl.textContent = "Выберите вариант.";
        return;
      }
      if (selected.dataset.correct === "true") {
        resultEl.textContent = "✅ Правильно!";
        sendResult(true);
      } else {
        resultEl.textContent = "❌ Неверно.";
        sendResult(false);
      }
    }

    function sendResult(success) {
      window.parent.postMessage({ type: "TASK_RESULT", success }, "*");
    }

    window.onload = renderOptions;
  </script>
</body>
</html>
<script>window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");</script>

=== 📄 task8.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Задание: 帝不我受佑</title>
</head>
<body>
  <h2>Что означает конструкция «帝不我受佑»?</h2>
  <p><strong>Обратите внимание на порядок слов.</strong></p>
  <div class="pinyin-container">
    <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
    <div class="pinyin-text">Dì bù wǒ shòu yòu</div>
  </div>
  <div id="answers"></div>
  <button onclick="check('answers', 'result', options)">Проверить</button>
  <p id="result"></p>
  <script>
    const options = [
      { text: "Небесный Владыка не пошлёт нам помощи", correct: true },
      { text: "Небесный Владыка пошлёт нам помощь", correct: false },
      { text: "Мы не получим урожай от Небесного Владыки", correct: false },
      { text: "Небесный Владыка не примет мою молитву", correct: false }
    ];

    function shuffle(array) {
      const arr = [...array];
      for (let i = arr.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [arr[i], arr[j]] = [arr[j], arr[i]];
      }
      return arr;
    }

    function renderOptions() {
      const shuffled = shuffle(options);
      const container = document.getElementById("answers");
      container.innerHTML = "";
      shuffled.forEach((opt, index) => {
        const label = document.createElement("label");
        const input = document.createElement("input");
        input.type = "radio";
        input.name = "q";
        input.value = opt.correct ? "1" : "0_" + index;
        input.dataset.correct = opt.correct.toString();
        label.appendChild(input);
        label.appendChild(document.createTextNode(opt.text));
        container.appendChild(label);
      });
    }

    function check(containerId, resultId, optionsArray) {
      const selected = document.querySelector('input[name="q"]:checked');
      const resultEl = document.getElementById(resultId);
      if (!selected) {
        resultEl.textContent = "Выберите вариант.";
        return;
      }
      if (selected.dataset.correct === "true") {
        resultEl.textContent = "✅ Правильно!";
        sendResult(true);
      } else {
        resultEl.textContent = "❌ Неверно.";
        sendResult(false);
      }
    }

    function sendResult(success) {
      window.parent.postMessage({ type: "TASK_RESULT", success }, "*");
    }

    window.onload = renderOptions;
  </script>
</body>
</html>
<script>window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");</script>

=== 📄 task9.html ===
<!DOCTYPE html>
<html lang="ru">
<head>
    <link rel="stylesheet" href="../../tasks.css">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Задание: 羌弗朕使烖</title>
</head>
<body>
  <h2>Какой из вариантов точно передаёт смысл фразы «羌弗朕使烖»?</h2>
  <p><strong>Обратите внимание на местоимение и отрицание.</strong></p>
  <div class="pinyin-container">
    <button class="pinyin-toggle" onclick="this.nextElementSibling.classList.toggle('expanded'); this.classList.toggle('expanded')">Показать пиньинь</button>
    <div class="pinyin-text">Qiāng fú zhèn shǐ zāi</div>
  </div>
  <div id="answers"></div>
  <button onclick="check('answers', 'result', options)">Проверить</button>
  <p id="result"></p>
  <script>
    const options = [
      { text: "Цяны не нанесут поражение моему послу", correct: true },
      { text: "Цяны нанесут поражение послу врага", correct: false },
      { text: "Мой посол не победит цянов", correct: false },
      { text: "Цяны не нападут на город", correct: false }
    ];

    function shuffle(array) {
      const arr = [...array];
      for (let i = arr.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [arr[i], arr[j]] = [arr[j], arr[i]];
      }
      return arr;
    }

    function renderOptions() {
      const shuffled = shuffle(options);
      const container = document.getElementById("answers");
      container.innerHTML = "";
      shuffled.forEach((opt, index) => {
        const label = document.createElement("label");
        const input = document.createElement("input");
        input.type = "radio";
        input.name = "q";
        input.value = opt.correct ? "1" : "0_" + index;
        input.dataset.correct = opt.correct.toString();
        label.appendChild(input);
        label.appendChild(document.createTextNode(opt.text));
        container.appendChild(label);
      });
    }

    function check(containerId, resultId, optionsArray) {
      const selected = document.querySelector('input[name="q"]:checked');
      const resultEl = document.getElementById(resultId);
      if (!selected) {
        resultEl.textContent = "Выберите вариант.";
        return;
      }
      if (selected.dataset.correct === "true") {
        resultEl.textContent = "✅ Правильно!";
        sendResult(true);
      } else {
        resultEl.textContent = "❌ Неверно.";
        sendResult(false);
      }
    }

    function sendResult(success) {
      window.parent.postMessage({ type: "TASK_RESULT", success }, "*");
    }

    window.onload = renderOptions;
  </script>
</body>
</html>
<script>window.parent.postMessage({ type: "TASK_EXISTS", valid: true }, "*");</script>

